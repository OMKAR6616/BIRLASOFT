This is the first line HTML CSSS GIT
git commands:

git init --- to initalize the git local repo

git remote add origin <url> --- to connect remote repo and local repo.

git station ----- to check the status of the file

git commit - m "message" to save the files in the local copy(git)

git push origin master - to push the commited changes to the remote copy.